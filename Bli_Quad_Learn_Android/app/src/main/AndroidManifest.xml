<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.harishram.bli_quad_learn">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bli_Quad_Learn">
        <activity android:name=".NotesDisplay"></activity>
        <activity android:name=".TakeNotes" />
        <activity android:name=".VideoScreen" />
        <activity android:name=".AudioPermission" />
        <activity android:name=".Materials" />
        <activity android:name=".Menu_2" />
        <activity android:name=".Intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu" />
    </application>

</manifest>